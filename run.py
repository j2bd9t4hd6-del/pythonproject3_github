import os
from flaskr import create_app # ★★★ create_app 関数をインポートする ★★★


# ----------------------------------------------------
# 1. 環境変数の設定 (Flask実行コマンドを使わないため、基本不要ですが残してもOK)
# ----------------------------------------------------
# os.environ['FLASK_APP'] = 'flaskr'
# os.environ['FLASK_ENV'] = 'development'


# ----------------------------------------------------
# 2. アプリケーションインスタンスの取得
# ----------------------------------------------------
app = create_app() # ★★★ create_app() 関数を呼び出して app インスタンスを生成 ★★★


# ----------------------------------------------------
# 3. サーバーの起動
# ----------------------------------------------------
if __name__ == '__main__':
    # Flaskの組み込みサーバーを起動します
    # __init__.py で app.debug = True に設定済みのため、debug=True は必須ではありませんが、付けておくと安心です
    app.run(debug=True, host='0.0.0.0', port=5000)
